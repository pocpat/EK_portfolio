// App.test.js — full App render requires @react-pdf-viewer/core which needs
// a browser DOM environment. Component-level tests cover the app instead.
// See NavBar.test.js, Projects.test.js, ProjectCard.test.js.

test('App test suite placeholder — full App render needs browser DOM', () => {
  expect(true).toBe(true);
});